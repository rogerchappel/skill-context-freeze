# Contributing

Keep contributions local-first and fixture-backed. New parsing behavior should include a focused test and a fixture or README example that shows the intended agent workflow.

Before opening a PR, run:

```sh
npm test
npm run check
npm run build
npm run smoke
```
